name: dbt CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  DBT_PROFILES_DIR: .
  DATABRICKS_HOST: ${{ DATABRICKS_HOST }}
  DATABRICKS_HTTP_PATH: ${{ DATABRICKS_HTTP_PATH }}
  DATABRICKS_TOKEN: ${{ DATABRICKS_TOKEN }}
  # Garante compatibilidade com runners modernos (Node 24)
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true 

jobs:
  dbt-lint:
    name: 🔍 SQL Lint & Style Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install SQLFluff
        run: |
          pip install sqlfluff sqlfluff-templater-dbt
      
      - name: Lint SQL files
        run: |
          cd airbnb_analytics
          sqlfluff lint models/ --dialect databricks --exclude-rules L034,L036
        continue-on-error: true # Não trava o pipeline se houver erros de estilo

  dbt-compile:
    name: 🔨 Compile dbt Models
    runs-on: ubuntu-latest
    needs: dbt-lint
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dbt
        run: |
          pip install dbt-databricks==1.7.0
      
      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<EOF
          databricks:
            target: dev
            outputs:
              dev:
                type: databricks
                catalog: workspace
                schema: airbnb_dev
                host: "${DATABRICKS_HOST}"
                http_path: "${DATABRICKS_HTTP_PATH}"
                token: "${DATABRICKS_TOKEN}"
                threads: 4
          EOF
      
      - name: Install dbt dependendecies
        run: |
          cd airbnb_analytics
          dbt deps
      
      - name: Compile dbt models
        run: |
          cd airbnb_analytics
          dbt compile --target dev
      
      - name: Upload compiled files
        uses: actions/upload-artifact@v4
        with:
          name: compiled-models
          path: airbnb_analytics/target/compiled/

  dbt-test:
    name: 🧪 Run dbt Tests
    runs-on: ubuntu-latest
    needs: dbt-compile
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dbt
        run: |
          pip install dbt-databricks==1.7.0
      
      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<EOF
          databricks:
            target: dev
            outputs:
              dev:
                type: databricks
                catalog: workspace
                schema: airbnb_dev
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 4
          EOF
      
      - name: Install dbt dependencies
        run: |
          cd airbnb_analytics
          dbt deps
      
      - name: Run dbt tests on sources
        run: |
          cd airbnb_analytics
          dbt test --select source:* --target dev
      
      - name: Run dbt tests on silver layer
        run: |
          cd airbnb_analytics
          dbt test --select silver --target dev
      
      - name: Generate test results report
        if: always()
        run: |
          cd airbnb_analytics
          dbt docs generate --target dev
      
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dbt-test-results
          path: airbnb_analytics/target/run_results.json

  dbt-build-silver:
    name: 🥈 Build Silver Layer
    runs-on: ubuntu-latest
    needs: dbt-test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dbt
        run: |
          pip install dbt-databricks==1.7.0
      
      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<EOF
          databricks:
            target: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
            outputs:
              dev:
                type: databricks
                catalog: workspace
                schema: airbnb_dev
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 4
              prod:
                type: databricks
                catalog: workspace
                schema: airbnb
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 8
          EOF
      
      - name: Install dbt dependencies
        run: |
          cd airbnb_analytics
          dbt deps
      
      - name: Build silver layer
        run: |
          cd airbnb_analytics
          dbt build --select silver --target ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}

  dbt-build-gold:
    name: 🥇 Build Gold Layer
    runs-on: ubuntu-latest
    needs: dbt-build-silver
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dbt
        run: |
          pip install dbt-databricks==1.7.0
      
      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<EOF
          databricks:
            target: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
            outputs:
              dev:
                type: databricks
                catalog: workspace
                schema: airbnb_dev
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 4
              prod:
                type: databricks
                catalog: workspace
                schema: airbnb
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 8
          EOF
      
      - name: Install dbt dependencies
        run: |
          cd airbnb_analytics
          dbt deps
      
      - name: Build gold dimensions
        run: |
          cd airbnb_analytics
          dbt build --select gold,tag:dimension --target ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
      
      - name: Build gold facts
        run: |
          cd airbnb_analytics
          dbt build --select gold,tag:fact --target ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}

  dbt-docs:
    name: 📚 Generate & Deploy Documentation
    runs-on: ubuntu-latest
    needs: dbt-build-gold
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dbt
        run: |
          pip install dbt-databricks==1.7.0
      
      - name: Create profiles.yml
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<EOF
          databricks:
            target: prod
            outputs:
              prod:
                type: databricks
                catalog: workspace
                schema: airbnb
                host: ${{ DATABRICKS_HOST }}
                http_path: ${{ DATABRICKS_HTTP_PATH }}
                token: ${{ DATABRICKS_TOKEN }}
                threads: 8
          EOF
      
      - name: Install dbt dependencies
        run: |
          cd airbnb_analytics
          dbt deps
      
      - name: Generate dbt docs
        run: |
          cd airbnb_analytics
          dbt docs generate --target prod
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4 # Atualizado para 
        with:
          github_token: ${{ GITHUB_TOKEN }}
          publish_dir: ./airbnb_analytics/target
          publish_branch: gh-pages